<UserControl x:Class="UedConverter.Components.FadingMark"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UedConverter.Components"
             mc:Ignorable="d" 
             Opacity="0" Visibility="Collapsed" d:Opacity="1" d:Visibility="Visible">
    <UserControl.Resources>
        <Storyboard x:Key="FadeOutStoryboard" Completed="FadeOutStoryboard_Completed">
            <DoubleAnimation Storyboard.TargetProperty="Opacity"
                         To="0" Duration="0:0:0.6" />
        </Storyboard>
    </UserControl.Resources>

    <Border Padding="4">
        <Path x:Name="CheckMark" Width="14" Height="14"
              SnapsToDevicePixels="False"
              Stroke="#FF43C943"
              StrokeThickness="2"
              Data="M 0 6 L 6 14 M 5 14 L 14 0"/>
    </Border>
</UserControl>
